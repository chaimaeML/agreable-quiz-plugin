<style>

    {# Common CSS #}
    {% include common_css_path %}

    {#
        Configurable CSS values provided through custom WP settings.
        Overrides values in common CSS.
    #}
    .answer {
      background-color: {{ primary_colour|default('ff00ff') }};
      color: {{ secondary_colour|default('black') }};
      font-family: {{ font_family }};
    }

    {# Also provided through custom WP settings. #}
    {{ extra_css }}

</style>
